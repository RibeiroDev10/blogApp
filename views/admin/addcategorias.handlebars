{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3>Nova Categoria</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/nova" method="POST">
            <label for="nome">Nome: </label>
            <input class="form-control" id="nome" type="texto" name="name" placeholder="Nome da categoria">
            <label for="slug">Slug: </label>
            <input class="form-control" type="texto" name="slug" id="slug" placeholder="Slug da categoria">
            <br>
            <button type="submit" class="btn btn-success">Criar categoria</button>
        </form>
    </div>
</div>